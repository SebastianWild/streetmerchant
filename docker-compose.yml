version: '3.0'
services:
  streetmerchant:
    image: sebaswild/streetmerchant:latest
    build:
      context: .
      dockerfile: Dockerfile
    container_name: streetmerchant
    env_file:
      - dotenv
